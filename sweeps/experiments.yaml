
program: main.py
name: all-experiments
method: grid
parameters:
  dataset:
    values: [mixed_irony]
  model:
    values: [llama, hatebert, gpt-2]
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}
