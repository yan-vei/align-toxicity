basic:
  use_wandb: False
  device_map: auto
  offload_folder: ./offload
  output_dir: results
  is_fewshot: True
defaults:
  - _self_
  - model: llama
  - dataset: mixed_irony
  - prompt: defaults
hydra:
  job:
    chdir: False
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S.%f}